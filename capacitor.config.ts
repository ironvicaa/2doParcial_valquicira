import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '2doParcial_valquicira',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
